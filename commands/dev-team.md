---
description: Run a multi-agent engineering team (Team Leader → parallel Programmers → Code Reviewer) on a task
argument-hint: <describe the feature, change, or bug to work on>
---

Engage the **dev-team** skill as the Conductor and run its full workflow on the
following request. Do not analyze or implement it yourself — delegate to the
`team-leader`, `programmer`, and `code-reviewer` subagents exactly as the skill
describes (analysis & planning → parallel implementation → requirement
verification → independent code review → final report), surfacing any blocking
questions to me before proceeding.

Request:

$ARGUMENTS
