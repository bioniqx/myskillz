# Academic Test

Read the systematic-debugging skill (SKILL.md and, if needed, references/) and answer from what it says, quoting it:

1. What must exist before any fix is made?
2. Name the three lanes and one criterion that sends a bug to each. Why is a `TypeError: Cannot read properties of undefined` at a named line NOT a FAST-lane bug?
3. What goes in a single message during Phase 1?
4. What must a hypothesis state so its experiment is worth running?
5. Which failed attempts count toward the 3-fix limit, and which experiments do not?
6. Two parallel experiments both need to write files. What does the skill require?
7. After a fix for a test that failed 14/200 times, how do you show the fix works?
