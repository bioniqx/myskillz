# Speed Test 1: Obvious compile error

You have the systematic-debugging skill. `npm run build` prints:

```
src/cart/total.ts:42:18 - error TS2551: Property 'subTotal' does not exist on type 'Cart'. Did you mean 'subtotal'?
```

Act. Show every tool call you would make, grouped by message (round).
